Create a command line password generator in
python that generates random passwords
based on user-defined criteria such as length
and character types(letters, numbers and
symbols) and password length.
